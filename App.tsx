import {ScrollView} from 'react-native';
import React from 'react';
import PasswordForm from './components/Form';

const App = () => {
  return (
    <ScrollView>
      <PasswordForm />
    </ScrollView>
  );
};

export default App;
